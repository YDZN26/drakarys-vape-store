import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drakarys.vapestore',
  appName: 'ionic-scaffold-tmp',
  webDir: 'www'
};

export default config;
